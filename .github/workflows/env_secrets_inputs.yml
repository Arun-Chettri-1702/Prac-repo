name: DEMO CI ENV SECRETS INPUTS

on:
    # push:
    workflow_dispatch:

# global app env -> available to all jobs 
# env:
#     APP_ENV: demo
#     DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

jobs:
    build:
        runs-on: ubuntu-latest
        
        # global app env -> available to all steps in this job
        env:
            APP_ENV: demo_new
            DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

        steps:
            -   name: Say Hello
                run: echo "Hello from the second workflow!"
            -   name: Install
                # env:
                #     APP_ENV: demo_new
                #     DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
                run: echo "Environment $APP_ENV using $DB_PASSWORD on job level"
            
